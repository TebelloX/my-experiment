{
  "name": "@bankjobs/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "db:local": "wrangler d1 execute DB --local --file=../../db/schema.sql && wrangler d1 execute DB --local --file=./scripts/seed-local.sql",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@bankjobs/core": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.8.71",
    "@cloudflare/workers-types": "4.20250906.0",
    "wrangler": "4.129.0"
  }
}
